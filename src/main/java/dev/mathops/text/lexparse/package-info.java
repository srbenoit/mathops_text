/**
 * A general lexical scanner ("lexer") that takes a string and a list of allowed token recognizers and produces a list
 * of tokens, and a general syntactic parser ("parser") takes a list of tokens and a list of allowed object recognizers
 * and produces a list of objects.
 */
package dev.mathops.text.lexparse;
